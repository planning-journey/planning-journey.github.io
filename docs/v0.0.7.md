npm run dev는 절대로 죽었다 깨어나도 실행하지마.
그냥 build 테스트만 하고 이상 없으면 넘어가.

그리고 목표 에디터 모달에서 캘린더는 외부 라이브러리 대신,
현재 앱의 디자인과 일치하는 컴포넌트를 직접 생성해.

캘린더는 주간 선택, 단일 날짜 선택, 시작일/종료일 선택이 가능해야하며,
각각 목표 기간 유형 주간, 일간, 자유에서 적용돼야해.
