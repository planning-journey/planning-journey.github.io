# v0.0.36

## 변경 사항
- **데이터베이스 최적화**: Dexie DB 스키마를 업데이트하여 `tasks` 및 `dailyEvaluations` 테이블에 복합 인덱스(`[date+projectId]`)를 추가했습니다. 이를 통해 날짜 선택 및 데이터 조회 성능이 향상되고 콘솔 경고가 해결됩니다.
- **타입 오류 수정**: `DragDropContext.tsx`에서 발생하는 `NodeJS.Timeout` 관련 타입 오류를 `ReturnType<typeof setTimeout>`으로 변경하여 수정했습니다.
