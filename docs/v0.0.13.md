인라인 캘린더에 여전히 문제가 있어.

날짜를 동적으로 생성하라는 말은 뷰포트에 보이는 날짜만 생성하라는 말이야.
지금은 날짜를 동적으로 컨테이너 내에 추가하는 형식인데, 이렇게 되면 HTML 요소 수가 많아지면서 오버헤드가 커져.
버추얼 스크롤링은 transform을 이용해 스크롤 위치를 조정하면서 표시할 내용만 렌더링 하잖아.
그것처럼 달력을 구현하라구.
