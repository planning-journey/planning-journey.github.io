OngoingGoalsHeader 에서 chevron 버튼을 눌러서 열면 
목표들 중에서 현재 InlineCalendar에 선택된 날짜가 `시작일 >= 현재 선택된 날짜 <= 종료일`인 목표 목록을 표시해줘.
이렇게 하려면 아마 InlineCalendar에서 선택된 날짜가 앱 전체 상태에서 관리가 돼야할거야.

목표 목록은 종료일이 느린 것부터 빠른 것 순서로 표시해줘.
