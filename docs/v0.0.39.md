# v0.0.39

## 변경 사항
- **날짜 처리 버그 수정**: `YYYY-MM-DD` 형식의 문자열을 `Date` 객체로 변환할 때 타임존 차이로 인해 날짜가 하루 어긋나거나 잘못 표시되던 문제를 수정했습니다.
- **목표 수정 로직 개선**: 목표 수정 모달에서 기존 데이터를 불러올 때 자동 계산 로직이 의도치 않게 작동하여 날짜가 덮어씌워지던 현상을 `isInitialLoad` 상태 관리를 통해 해결했습니다.
