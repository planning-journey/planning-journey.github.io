# v0.0.41

## 변경 사항
- **배포 설정 최적화**: GitHub 기본 제공 도메인(`${username}.github.io/planning-journey/`)에 맞춰 `vite.config.ts`의 `base` 설정을 추가했습니다.
- **배포 가이드 업데이트**: 커스텀 도메인이 아닌 GitHub 기본 도메인과 GitHub Actions를 이용한 자동 배포 방식으로 가이드를 전면 수정했습니다.
