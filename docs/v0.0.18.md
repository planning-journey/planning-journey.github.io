지금 InlineCalendar.tsx에서 아래 부분을 보면 스크롤 위치에 따라 적절한 연도와 월을 표시하고 있잖아.

```tsx
<div className="py-2 px-4 text-center text-sm font-semibold text-gray-800 dark:text-gray-100">
  {currentMonthYear}
</div>
```

이것처럼 Header.tsx의 아래 부분에도 스크롤 위치에 따라 적절한 연도와 월을 표시하게 해달라고.
지금은 스크롤을 해도 InlineCalendar.tsx의 currentMonthYear만 변경되지, Header.tsx의 currentMonthYear은 변경 안돼.

```tsx
<div className="text-xl font-bold text-gray-900 dark:text-white"> {/* Month/year display */}
  {currentMonthYear}
</div>
```
