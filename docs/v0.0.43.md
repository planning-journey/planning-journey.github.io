# v0.0.43

## 변경 사항
- **무한 루프 방지 로직 추가**: `404.html`에서 잘못된 경로 접근 시 발생할 수 있는 무한 리다이렉트 루프를 방지하는 검사 로직을 추가했습니다.
- **루트 도메인 배포 최적화**: `${username}.github.io` 형식의 루트 배포 환경에 맞춰 `pathSegmentsToKeep` 설정을 0으로 조정하고 관련 가이드를 업데이트했습니다.
