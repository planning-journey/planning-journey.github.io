# 작업 내역 (2026-01-07)

## 요청 사항
- 목표 수정 시 날짜가 잘못 표시되는 문제 해결 (예: 2026-01-05 ~ 01-08 목표가 2025-12-01로 표시됨).

## 수행 내용
1. **날짜 파싱 유틸리티 수정 (`src/utils/dateUtils.ts`)**:
   - `new Date(string)` 방식이 타임존(UTC) 문제로 날짜 어긋남을 유발하므로, 문자열을 분리하여 `new Date(y, m, d)` 방식으로 파싱하도록 수정했습니다.
2. **GoalEditorModal 로직 강화**:
   - `isInitialLoad` 상태를 도입하여 모달이 처음 열리고 기존 데이터를 로드할 때는 `startDate`, `endDate` 자동 계산 로직이 실행되지 않도록 차단했습니다.
   - `useEffect` 의존성 배열을 정리하여 불필요한 재계산을 방지했습니다.

## 결과
- 목표 수정 시 기존에 저장된 시작일과 종료일이 타임존 영향 없이 정확하게 표시됩니다.
