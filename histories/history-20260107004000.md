# History - 2026-01-07

## 작업 내용

`GoalEditorModal`에서 목표를 생성하거나 수정할 때, 날짜를 `YYYYMMDD` 형식의 문자열로 저장하도록 변경했습니다.

1.  **`src/components/GoalEditorModal.tsx` 수정**
    *   `startDate`와 `endDate`를 저장할 때 `formatDateToYYYYMMDD` 유틸리티 함수를 사용하도록 수정.
    *   `YYYYMMDD` 형식의 문자열을 다시 `Date` 객체로 변환하기 위한 `parseYYYYMMDD` 헬퍼 함수 추가.
    *   목표 수정 시 기존의 ISO 형식이 아닌 `YYYYMMDD` 형식의 데이터를 올바르게 파싱하여 폼에 반영하도록 수정.
    *   `crypto.randomUUID()`를 프로젝트 내 다른 곳에서 사용 중인 `uuidv4()`로 통일하여 일관성 유지.

## 결과

목표 데이터의 날짜 형식이 `YYYYMMDD`로 통일되어, 데이터베이스 조회 및 다른 컴포넌트(예: GoalSelectionBottomSheet)와의 연동이 더 효율적이고 안정적으로 이루어집니다.
