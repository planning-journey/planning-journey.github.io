### User Request:
너 기능을 잘못 구현했어. TaskEditorModal에서 그 작업을 처리할 게 아니라, GoalSelectionBottomSheet에서 목표가 선택되고 나면 할 일 입력 폼의 인풋에 포커스를 해야하는거야.

### Plan:
1.  **Revert Previous Changes:**
    *   Revert the `handleSave` function in `TaskEditorModal.tsx` to its original state.
    *   Remove the `taskTextInputRef` attachment from the input element in `TaskEditorModal.tsx`.
    *   Remove the `taskTextInputRef` declaration from `TaskEditorModal.tsx`.
2.  **Identify Correct Components:**
    *   Determined that `src/App.tsx` orchestrates the interaction between `DailyDetailForm` (the "할 일 입력 폼") and `GoalSelectionBottomSheet`.
    *   `DailyDetailForm.tsx` contains the actual input field that needs to be refocused.
3.  **Implement Refocusing Logic:**
    *   **Modify `DailyDetailForm.tsx`:**
        *   Refactor `DailyDetailForm` to use `React.forwardRef`, allowing parent components to pass a ref to its input element.
        *   Attach the forwarded `ref` to the `<input type="text">` element within `DailyDetailForm.tsx`.
    *   **Modify `App.tsx`:**
        *   Import `useRef` from `react`.
        *   Declare `dailyDetailFormInputRef = useRef<HTMLInputElement>(null);`.
        *   Pass this `dailyDetailFormInputRef` to the `DailyDetailForm` component as a prop.
        *   Modify the `handleSelectGoal` function (which is called after a goal is selected in `GoalSelectionBottomSheet`) to programmatically focus the `DailyDetailForm`'s input using `dailyDetailFormInputRef.current.focus()` after the new task is added and `currentTaskText` is cleared.

### Execution:
1.  **Reverted `TaskEditorModal.tsx`:**
    *   Restored `handleSave` to its original state, ensuring the modal closes after any save operation.
    *   Removed `ref={taskTextInputRef}` from the input element.
    *   Removed the `taskTextInputRef` declaration.
2.  **Modified `src/components/DailyDetailForm.tsx`:**
    *   Changed the component definition to `const DailyDetailForm = React.forwardRef<HTMLInputElement, DailyDetailFormProps>(({ onAddTask }, ref) => { ... });`.
    *   Attached `ref={ref}` to the `<input type="text">` element.
3.  **Modified `src/App.tsx`:**
    *   Added `useRef` to the `import` statement.
    *   Declared `const dailyDetailFormInputRef = useRef<HTMLInputElement>(null);`.
    *   Passed `ref={dailyDetailFormInputRef}` to the `DailyDetailForm` component: `<DailyDetailForm onAddTask={handleAddTask} selectedDate={selectedDate} ref={dailyDetailFormInputRef} />`.
    *   Updated the `handleSelectGoal` function to include:
        ```typescript
        if (dailyDetailFormInputRef.current) {
          dailyDetailFormInputRef.current.focus();
        }
        ```
        after `setCurrentTaskText('');`.

### Conclusion:
The functionality to refocus the task input form after selecting a goal from the `GoalSelectionBottomSheet` has been correctly implemented. This ensures a smoother workflow for users adding new tasks, as the input field is automatically ready for the next entry. The previous incorrect implementation in `TaskEditorModal.tsx` has been fully reverted.
