# History - 2026-01-07

## 작업 내용

`GoalSelectionBottomSheet`에서 목표를 선택할 때, 현재 선택된 날짜(`selectedDate`)를 포함하는 기간의 목표만 표시되도록 필터링 로직을 추가했습니다.

1.  **`src/components/GoalSelectionBottomSheet.tsx` 수정**
    *   `selectedDate: Date`와 `selectedProjectId: string | null`을 Props로 추가.
    *   `useLiveQuery` 내에서 `db.goals`를 조회할 때 `projectId`로 필터링하고, `startDate`와 `endDate` 사이에 `selectedDate`가 있는지 확인하는 로직 추가.
    *   필터링된 결과가 없을 경우 "해당 날짜에 활동 중인 목표가 없습니다." 메시지 표시.
    *   "선택 안함" 항목은 필터링 결과와 상관없이 항상 표시되도록 유지.

2.  **`src/App.tsx` 수정**
    *   `GoalSelectionBottomSheet` 컴포넌트에 `selectedDate`와 `selectedProjectId` Props 전달.

## 결과

사용자가 특정 날짜의 할 일을 추가할 때, 해당 날짜에 진행 중인 목표만 선택 목록에 나타나게 되어 데이터 정합성을 높이고 사용자 경험을 개선했습니다.
