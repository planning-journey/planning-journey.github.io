# History - 2026-01-07

## 작업 내용

`TaskItem` 컴포넌트에서 할 일에 연결된 목표가 완료된 상태일 경우, 목표 이름을 시각적으로 구분할 수 있도록 수정했습니다.

1.  **`src/components/TaskItem.tsx` 수정:**
    *   연결된 `goal`의 `status`가 `'completed'`인 경우, 목표 이름 표시 부분에 `line-through`(취소선) 스타일과 흐릿한 색상을 적용했습니다.
    *   목표 색상을 나타내는 작은 점(indicator)에도 `opacity-50`을 적용하여 완료된 느낌을 주었습니다.

## 결과

사용자는 할 일 목록에서 해당 할 일이 연결된 목표가 이미 완료되었는지 여부를 직관적으로 확인할 수 있게 되었습니다.
