# 작업 히스토리 - 2026-01-09 (5)

## 작업 내용
목표 선택 Bottom sheet(`GoalSelectionBottomSheet`)에서 키보드(방향키)로 항목을 이동할 때, 현재 선택된 항목이 리스트의 중앙에 오도록 자동 스크롤 기능을 추가했습니다.

### 주요 변경 사항
1.  **자동 스크롤 구현** (`src/components/GoalSelectionBottomSheet.tsx`)
    *   `itemRefs` (`useRef`) 배열을 생성하여 리스트의 각 항목(`li`)에 대한 참조를 저장했습니다.
    *   `useEffect`를 사용하여 `activeIndex`가 변경될 때마다 해당 인덱스의 항목 요소를 찾아 `scrollIntoView({ block: 'center', behavior: 'smooth' })`를 호출하도록 구현했습니다.
    *   이를 통해 키보드로 긴 목록을 탐색할 때 포커스된 항목이 항상 시야의 중앙에 유지되어 사용자 경험(UX)이 향상되었습니다.

### 관련 파일
- `src/components/GoalSelectionBottomSheet.tsx`

### 커밋 정보
- **Message**: `feat: auto-scroll to centered item in GoalSelectionBottomSheet`
- **Hash**: `85bb28a` (Local)
