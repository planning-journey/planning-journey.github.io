# 작업 히스토리 - 2026-01-09

## 작업 내용
목표 수정 모달(`GoalEditorModal`)에서 달력 선택 외에도 날짜를 직접 타이핑하여 입력할 수 있는 기능을 추가했습니다.

### 주요 변경 사항
1. **DateInput 컴포넌트 신규 생성** (`src/components/DateInput.tsx`)
   - `YYYY-MM-DD` 및 `YYYYMMDD` 형식의 입력을 지원하며, 자동으로 포맷팅을 수행합니다.
   - 포커스를 잃거나(onBlur) Enter 키를 누를 때 유효성 검사를 수행합니다.
2. **GoalEditorModal 연동** (`src/components/GoalEditorModal.tsx`)
   - 기간 설정 유형이 `daily`일 경우 선택된 날짜 입력 필드 노출.
   - 기간 설정 유형이 `free`일 경우 시작일과 종료일 입력 필드 노출.
   - 달력과 입력 필드가 상호 동기화되도록 구현.

### 관련 파일
- `src/components/DateInput.tsx` (신규)
- `src/components/GoalEditorModal.tsx` (수정)

### 커밋 정보
- **Message**: `feat: add manual date input to GoalEditorModal`
- **Hash**: `4341aba` (Local)
